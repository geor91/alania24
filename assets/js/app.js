$(document).ready(function() {
	// init slider on gallery page
	$('#sliderID').owlCarousel({
		pagination: false,
		navigation: true,
		navigationText: false,
		slideSpeed: 400,
		singleItem:true
	});
});